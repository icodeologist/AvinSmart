# Use a same-origin path when the frontend is served with the API.
VITE_API_BASE_URL=/api/v1

# Only needed when running Vite against a separately hosted backend.
# VITE_API_BASE_URL=http://localhost:8080/api/v1
