FROM golang:1.23-alpine AS build

WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w" -o /out/avinsmart ./cmd/server

FROM alpine:3.20

RUN addgroup -S avinsmart && adduser -S -G avinsmart avinsmart
WORKDIR /app
COPY --from=build /out/avinsmart /app/avinsmart
USER avinsmart
EXPOSE 8080
ENTRYPOINT ["/app/avinsmart"]
